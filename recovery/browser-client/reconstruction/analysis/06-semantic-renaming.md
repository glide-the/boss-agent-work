# 06 Semantic Renaming

Semantic names live under `maps/`. Every mapping must include source, anchor or range, target module, classification, confidence, evidence and verification.

High confidence requires at least one direct export/API/error anchor and one independent call/data-flow or runtime check. Names created only for maintainability are classified `reconstructed`, never presented as author names.
