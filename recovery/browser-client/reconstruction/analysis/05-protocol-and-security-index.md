# 05 Protocol and Security Index

`protocol-strings.csv` indexes JSON-RPC markers, environment variables, command/enum values, URLs and security/error text with source ranges.

`security-call-graph.json` selects calls and strings related to URL validation, origin consent, site status, uploads/downloads, file transfer, CDP and clipboard. Static presence does not prove ordering; ordering requires differential tests.
