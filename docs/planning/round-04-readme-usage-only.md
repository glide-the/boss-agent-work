# Round 04：README 使用版精简

## Optimized Prompt

精简 `/Users/dmeck/project/boss-agent-work/develop/README.md`，将其从工程技术首页调整为面向普通用户的 Boss投递使用说明。只保留产品用途、使用前准备、首次连接、岗位读取与筛选、沟通草拟、批量处理、定时任务、安全确认、连接排查和相关文档入口。删除目录结构、构建命令、Native Messaging 架构、身份参数、开发历史细节和同类插件实现方法。

将用户提供的三张实际运行截图复制到仓库 `docs/images/` 并嵌入 README，分别说明 Chrome 已连接、Codex 批量处理和定时任务三个使用场景。所有示例提示词必须可以直接复制；默认先读取和草拟，任何沟通、发送或投递动作均需用户确认。插件连接失败时明确停止，不改用其他浏览器自动化工具伪装成功。

## Optional Enhancers

- 已采用：用“准备 → 连接 → 只读测试 → 批量处理 → 定时任务 → 排障”组织内容。
- 已采用：技术资料只保留简短链接，不在 README 展开。
- 已采用：三张图片使用稳定的仓库相对路径，不依赖临时附件目录。

## 完成条件

- README 主要回答“怎么安装、怎么开始、怎么下指令、失败怎么办”。
- 三张截图均能通过仓库相对路径显示。
- 详细安装、开发复线和同类插件指南仍保留在 `docs/`。
- Markdown 链接、图片路径和差异检查通过。

## 执行结果（2026-08-17）

- settled：README 已缩减为 116 行，只保留安装入口、使用示例、批量处理、定时任务、确认边界和连接排查。
- settled：三张 PNG 已复制到 `docs/images/`，README 的图片和文档相对链接均已验证。
- settled：技术架构、构建命令、身份参数与开发实现均不再在 README 展开，详细资料继续保留在 `docs/`。
- settled：`git diff --check` 通过。
