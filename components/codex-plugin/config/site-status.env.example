# Disabled by default. This preserves URL validation, origin consent, and file-transfer checks.
BROWSER_USE_SITE_STATUS_CHECK_ENABLED=false

# Used only when the dedicated check is explicitly enabled. Loopback addresses only.
BROWSER_USE_SITE_STATUS_BASE_URL=http://127.0.0.1:8787
