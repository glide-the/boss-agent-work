# Chrome File Upload Troubleshooting
If file upload fails while setting files through a file chooser, tell the user exactly this:

`To enable file upload, go to chrome://extensions in Chrome, click Details under the Boss投递 Chrome extension, and enable "Allow access to file URLs."`
